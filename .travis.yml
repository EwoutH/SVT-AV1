language: c
dist: xenial

matrix:
  include:
    - dist: trusty
      compiler: gcc
    - dist: trusty
      compiler: clang
    - os: osx
      osx_image: xcode10.1

addons:
  apt:
    packages:
     - cmake
     - yasm

script:
 - cd Build/linux
 - ./build.sh release
