language: c

matrix:
  include:
    - dist: xenial
      compiler: gcc
    - dist: xenial
      compiler: clang
    - os: osx
      osx_image: xcode10.1

addons:
  apt:
    packages:
     - cmake
     - yasm

script:
 - cd Build/linux
 - ./build.sh release
